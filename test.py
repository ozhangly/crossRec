from utility.parser import *
from tqdm import tqdm

# parser = arg_parse()
#
# num_neighbours = parser.numofneighbours
#
# print(f'修改前: parser.num_neighbours: {num_neighbours}')
# print(f'修改前: num_neighbours: {num_neighbours}')
#
# num_neighbours = 30
# # 验证修改parser中的属性是否可以
# parser.numofneighbours = 30
#
# print(f'修改后: parser.num_neighbours: {parser.numofneighbours}')
# print(f'修改后: num_neighbours: {num_neighbours}')

# with open(file='metadata/lib_info.csv', mode='r') as fp:
#     for line in tqdm(fp.readlines()):
#         line = line.strip('\n')
#         print(line)
